[build]
  publish = "."
  functions = "netlify/functions"

[functions."cloudbeds"]
  timeout = 26

[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "no-store, no-cache, must-revalidate, max-age=0"

# Short teacher-view links (one per event) now live in the _redirects file.
